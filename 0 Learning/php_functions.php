<?php

// require or require_once call a php file in current script
// it directly execute if normal script but if it has function
// or Class then you have to explisitely call it.

use Illuminate\Contracts\Http\Kernel;

/*
|--------------------------------------------------------------------------
| Check If The Application Is Under Maintenance
|--------------------------------------------------------------------------
| If the application is in maintenance / demo mode via the "down" command
*/

define('LARAVEL_START', microtime(true));

if (true) {
    //
}

file_exists($maintenance = __DIR__.'/../storage/framework/maintenance.php');

require "./path/to/file";

PHP_VERSION_ID;
PHP_EOL;
PHP_SAPI;
STDERR;
E_USER_ERROR;
PATH_SEPARATOR;

headers_sent();

header('HTTP/1.1 500 Internal Server Error');

ini_get('display_errors');

fwrite(STDERR, $err);

echo $err;

trigger_error($err, E_USER_ERROR);

require_once __DIR__ . '/composer/autoload_real.php';


/**
 * @return \Composer\Autoload\ClassLoader
 */

class className {

    private static $loader;

    public function __construct()
    {
        // code
    }

    public static function method()
    {
        # code...
        self::$loader;
    }
}

className::method();

str_replace('search', 'replace', 'orginalstring');

__DIR__;

spl_autoload_register();


